{% extends 'base.html.twig' %}

{% block body %}

        {{form_start(form)}}
        <div class="form-group">
        {{form_widget(form)}}

            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <input type="submit" class="btn btn-download" value="Створити">
            </div>
        </div>
        {{form_end(form)}}

{% endblock %}